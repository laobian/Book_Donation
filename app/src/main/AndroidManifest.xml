<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.wx.donation_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_donation"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Register1Activity"></activity>
        <activity android:name=".activity.DonateSuccessActivity" />
        <activity android:name=".activity.ThanksDonationActivity" />
        <activity android:name=".activity.ProjectDetailActivity" />
        <activity android:name=".activity.DonateActivity" />
        <activity android:name=".activity.MyAchievementActivity" />
        <activity android:name=".activity.LoveValueActivity" />
        <activity android:name=".activity.MyDonationActivity" />
        <activity android:name=".activity.SearchActivity" /> <!-- 启动activity -->
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PersonInfoActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.LoginActivity" />
    </application>

</manifest>